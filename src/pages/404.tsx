import * as React from "react"

// styles
const pageStyles = {
  color: `#232129`,
  padding: `96px`,
  fontFamily: `-apple-system, Roboto, sans-serif, serif`,
}

// markup
const NotFoundPage: React.FC = () => {
  return <main style={pageStyles}>Hello World</main>
}

export default NotFoundPage
